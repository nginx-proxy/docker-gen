{{/* This template is used to test and debug the template engine. */}}
{
  "Docker": {{ toPrettyJson .Docker }},
  "Env": {{ toPrettyJson .Env }},
  "CurrentContainer": {{ toPrettyJson .CurrentContainer }},
  "Containers": {{ toPrettyJson . }}
}
